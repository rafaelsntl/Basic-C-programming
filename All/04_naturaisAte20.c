#include <stdio.h>

int main()
{
int fat, n, num;
printf("Calculando os fatoriais até 20");


for(n = 20; n > 1; n -- ) {
Sn = n  (a1 - an)/2 }

printf("\n\n\nFatorial calculado: %d", fat);
return 0;
}
